def delta_weight(eta, y_act, y_pred, x):
    """Computes the delta for weight update.
    The delta is the result of eta (learning rate) times
    the difference between the actual class and the predicted 
    class (y_act - y_pred), times x (the input).  
    """
    #TODO implement this function 

