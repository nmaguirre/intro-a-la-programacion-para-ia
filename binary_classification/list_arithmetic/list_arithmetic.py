def sum_prod(ls1, ls2):
    """Computes the sum of products of two lists of numbers of the same length.
    ls1 and ls2 are two lists of numbers, of the same length. This function
    computes sum (ls1[i] * ls[i]), for 0 <= i < len(ls1). 
    """
    #TODO implement this function


